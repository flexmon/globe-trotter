You must always ask for explicit permission from the USER before running any commands related to a Docker build or a Kubernetes/container deployment.
This includes `docker build`, `docker push`, `kubectl rollout`, `kubectl apply`, `helm install`, etc.
Wait for the user's approval before executing these commands.
